# 六都市長候選人永續韌性城市政策承諾 -- 一頁式倡議網站

## 目標
為淨零觀測站小組製作獨立的一頁式倡議網站，動態有趣。內容未齊的部分先用 placeholder／生成式內容呈現排版與設計語言。

## 素材（已讀取，存於 scratchpad）
- 主構想 doc「六都倡議網站」(1enPCJyU3m1jTYTGWWMp4oMcd857lcLS3JoNPCHD2GyA)
  - 分頁1：網站架構構想；資料蒐集分頁：六都重要性數據（人口 69.9%、用電 65%、碳排 70%+）
  - Comments：簽署名單「類似人本交通網站」；五大訴求「希望可以點開展開」
- 第二層 doc「邀請六都市長候選人進行政策交流及回應民間訴求」(1h-LEbmhw8MuAL9t9JqLFKy430ftGnKxUgyDxhoe4Ztc)
  - 邀請信（九發起團體、時程 8/12 記者會→9/11 回覆→9/18 拜會→9/28 簽署→10月初公布）
  - 附件一：五大訴求 18 項完整文字（全文已有，非 placeholder）
  - 附件二：承諾書回函（候選人逐項勾選）→ 簽署結果可做到「per-candidate per-訴求」粒度
- 試算表「全台縣市高溫風險數據整理」(1uUUZHqTtvYOS2vgG-sg3GZ-w5UuAQrwPAAYn09zkhc8)
  - 工作表1（縣市×暖化情境高溫天數增幅、長者人口、熱傷害）＋3個子表
- 主視覺參考圖 keyvisual_ref.png：米色底、紫漸層標題、綠藍城市天際線線稿、投票箱 icon

## 網站架構（doc 構想）
關於我們（九團體）、六都重要性（數據）、五大訴求（可展開 18 項）、簽署結果、連署結果、加入連署
Slogan:「面對城市的下一個十年，六都市長準備好了嗎？」

## 決策（2026-08-27 Joseph 回覆）
- 設計基調：以行動主視覺為準（米色底、紫漸層、綠藍天際線、投票箱），不延續兩個觀測站的視覺
- 資料更新：網站直讀已發布的 Google Sheet（載入時抓取），小組改表即更新
- 連署：站內表單（送 Apps Script 寫回 Sheet，人數隨頁面載入更新）；對象＝公民團體＋一般公民個人
- 網域：gcaa.org.tw 子網域；部署 GitHub Pages（Vite 靜態站，之後要搬 Vercel 成本近零）
- 簽署看板：全部列出六都候選人，含未回應者（施壓機制）
- 動畫強度：重點段落（hero、六都數據段）滾動敘事，其餘進場動畫＋數字滾動
- Placeholder 政策：候選人名單用明確標示的示意資料；三大數據（人口 69.9%、用電 65%、碳排 70%+）與高溫資料用 doc/試算表真實數字

## 設計筆記（frontend-design）
- 簽名元素：天際線是「有生命的城市」──hero 描邊畫出，數據段隨敘事變化（人口→亮起、用電→窗閃、碳排→煙散），footer 反白收尾。誇飾集中在此，其他段落收斂
- 米色底來自主視覺（非 AI 預設的選擇）；辨識度靠 jf-openhuninn＋紫漸層標題、天際線母題、投票箱微互動（CTA hover 時選票滑入）
- 字型決策：數字也用 huninn（圓體 Latin 同家族），不另引 mono
- 已知風險：woff2 全字集 2.1MB，正式上線前可 subset（教育部常用字＋內容用字）

## Phase 1-4 完成紀錄（2026-08-27）
- What：網站完成並上線 https://litostswirrl.github.io/six-counties/ 。九個段落全數實作；29 個測試通過；375/1280 寬度與表單送出流程驗證完畢；GitHub Actions 自動部署
- Why 重要決策：
  - 資料層 demo fallback：SHEET_ID/APPS_SCRIPT_URL 留空即示意模式，畫面明確標示「示意資料」；填入即切換真實資料，不用改元件
  - 滾動敘事用 sticky + IntersectionObserver sentinels 而非 GSAP pin（更穩、mobile 直接退化為靜態卡片）；GSAP 只用在 hero 視差與進場
  - 高溫圖表為 sequential 紫色 ramp（情境是有序強度非類別），最淺紫對米色底對比不足以 bar 端直接標值補救（dataviz validator 結論）
  - 原文照搬原則（Joseph 中途指正）：構想 doc／附件一的文字逐字進 content 模組，humanizer 只處理我們生成的過場與介面文案
- Next／待辦（需要人工或小組動作）：
  - 建正式 Google Sheet 四工作表＋Apps Script 部署（指南在 docs/apps-script-deploy.md），填 src/data/config.ts 兩值後重佈
  - gcaa.org.tw 子網域：repo Settings→Pages 設 custom domain＋DNS CNAME；vite.config.ts base 改 '/'
  - repo 目前在個人帳號（LitostSwirrl），要移轉到 gcaa-org-tw org 可用 gh repo transfer
  - OG image 待主視覺完稿；woff2 2.1MB 上線前可 subset；簽署狀態四分類需與詩婷對齊
  - 高溫資料來源：試算表內無出處註記，HEAT_SOURCE 暫寫「發起團體彙整」，發布前要跟建表同事確認

## Phase 5 修訂第一輪（進行中，2026-08-27）
- 已完成：nav 通欄＋大字兩色標題＋scrollspy；六都數據改單一畫面三欄；天際線 v2（六都地標：101、雙子星、機場塔台、摩天輪、廟宇、85 大樓、港區起重機＋水塔騎樓等台式細節）；五訴求圖示 v2；全站字級 html 17px 基準；資訊性標籤去 pill 改 tag-box（主視覺方框語言），按鈕保留圓角
- 進行中：兩個 Opus agents 研究六都可查證數據（基本盤＋五訴求面向指標，要求附來源 URL）；規劃「六都體檢」互動段：五訴求 tab × 六都比較 bar chart（切換動畫＋hover），資料檔 src/content/cityData.ts 待 agents 回報後建立
- 試算表補充資料已讀：8月熱傷害人次（至 8/16，台中 61 最高）；該分頁同時有各縣市人口
- 資料範圍修正（Joseph 指示）：數據段只用專案既有素材（doc 資料蒐集分頁＋高溫試算表），不外部搜尋；已停止兩個研究 agents，其（含子查詢）結果一律不採用
- 「以高溫為例」互動段完成：暖化情境切換（1.5/2/3/4 度）×六都／全台 22 縣市切換，長條排序滑動動畫＋中位數虛線（中位數取自試算表）；副圖：六都 65+ 佔比（全國 20.54% 參考線）、8 月熱傷害人次；質性卡片：各都調適計畫是否列高溫為主要風險（逐字照搬試算表）
- 資料判讀決定：增幅系列採「工作表1」與「年高溫天數增幅」分頁（兩者一致）；「年高溫推估天數」分頁與其自身註記（推估=基期+增幅）對不上（臺北 1.5 度：12.2 ≠ 6.6+7.5），該分頁數字不上站，待與建表同事確認
- 氣候風險矩陣（Joseph 指示工作表1 A:D 為重點）：七類災害×六都圖示矩陣，置於高溫互動圖之前；調適計畫原文逐字收在展開區。詞彙歸類判斷：多雨/少雨→強降雨＋乾旱、水災→淹水、土砂→坡地、極端降雨→強降雨；原文完整保留可供查核

## 進度
- [x] 本次修改：加入連署入口改為指定 Google 表單、首頁統計改為候選人與團體連署資訊、氣候風險標題更新、頁尾移除募捐資訊
- [x] 本次資產審查：以瀏覽器檢查桌面與 390px 手機畫面；五大訴求與投票箱圖示重畫為一致線寬的向量構圖；首頁、資料段與頁尾改用低細節寬幅城市插畫 `public/images/city-skyline.webp`
- [x] 本次驗證：`npm run test` 33 項通過；`npm run build` 通過；手機與桌面無水平溢出；所有加入連署入口指向指定 Google 表單；站內連署表單已移除；頁尾不含募捐文字
- [x] 發布前驗證（2026-08-31）：`npm run test` 33 項通過；`npm run build` 通過；`git diff --check` 通過；本機 390px 與 1200px 檢查無水平溢出，3 個加入連署入口均指向指定 Google 表單
- [x] 已提交並部署（2026-08-31）：提交 `39079d2` 已推送 `main`；GitHub Actions 建置與 GitHub Pages 部署成功；公開網址已完成桌面與手機檢查
- [ ] 待人工確認：正式團體連署資料上線後，首頁團體數量與團體名稱是否應由同一份正式資料表同步
- [x] 讀取主 doc、comments、第二層 doc、試算表樣本、主視覺圖
- [ ] 四個參考網站分析（agents 執行中：還路於民、下一代人本交通、淨零觀測站、排碳大戶觀測站）
- [ ] 向 Joseph 提問釐清方向
- [ ] 提案 2-3 個設計方向
- [ ] 設計文件與 spec
- [ ] 實作（frontend-design skill；文案過 humanizer-zh-tw）

## Phase 5 修訂第二輪（本機驗證完成，待公開部署，2026-08-31）
- What：天際線改為左北右南排列的臺灣城市插畫，只保留首頁一張，改以原始比例顯示；首頁與連署區加入六筆明確標示為示意的團體資料，連署區以桌面雙欄、手機單欄呈現完整名單；首頁欄位改稱「連署團體」；top nav 的「加入連署」改連到 `#join`，加入連署區內按鈕仍連到指定 Google 表單；五個訴求圖示與投票箱重新繪製。
- Why：回應天際線地理順序、屋頂元素比例、重複與裁切、團體名單可讀性、導覽入口和圖示辨識度的修訂要求；示意資料與數量共用同一份資料，正式試算表有資料時仍保留正式資料流程。
- 驗證：`npm run test` 37 項通過；`npm run build` 通過，僅有既有字型路徑警告；`git diff --check` 已修正檢查點文件尾端空白；本機 390px 與 1200px 瀏覽器檢查確認天際線只出現一次、未使用裁切、導覽錨點與表單連結正確，手機穩定畫面無水平溢出。
- Next：提交並推送 `main`，確認 GitHub Actions 與公開網址，再補記部署結果；正式團體資料到位後，人工確認數量與完整名稱。

## Phase 5 頁尾夜間天際線追加（2026-08-31）
- What：恢復頁尾天際線，新增同一構圖的夜間版 `public/images/city-skyline-night.webp`；`CityBackdrop` 增加日間／夜間素材切換，首頁使用日間版，頁尾使用夜間版。
- Why：保留頁尾天際線的收尾作用，同時讓頁尾與首頁有清楚的日夜視覺區分；不重新引入資料段的重複圖片。
- 驗證：`npm run test` 38 項通過；`npm run build` 通過，僅有既有字型路徑警告；`git diff --check` 通過；公開網址 390px 與 1200px 均確認兩張天際線完整載入、原始比例顯示、無水平溢出，頁尾夜間圖實際存在。
- 發布：提交 `e4de681` 已推送 `main`；GitHub Actions 工作流程 `33385157680` 的建置與部署均成功；公開網址為 https://litostswirrl.github.io/six-counties/?rev=e4de681。
- Next：等待 Joseph 指定下一個明確範圍；正式團體資料到位後，人工確認數量與完整名稱。

## Phase 6 SVG 天際線比例與夜色銜接（本機驗證完成，待公開部署，2026-08-31）
- What：以既有 SVG 天際線元件取代首頁與頁尾的點陣圖天際線；保留目前地標配置示例，左至右為臺北 101、桃園機場塔台、臺中國家歌劇院、臺南歷史建築、高雄 85 大樓與港區起重機；加入少量光電板、水塔與一般屋頂；日間與夜間使用同一 SVG 的兩種色彩變體。
- Why：恢復 SVG 時期的 `1440×300` 區塊比例與透明背景，移除大面積天空材質；夜間版直接使用頁尾紫色背景，消除夜色與頁尾區塊的落差。
- 驗證：`npm run test` 37 項通過；`npm run build` 通過，僅有既有字型路徑警告；`git diff --check` 通過；本機 1200px 與 390px 檢查確認兩個 SVG 比例正確、首頁不再載入點陣圖、頁尾使用夜間變體、無水平溢出。
- Next：提交並推送 `main`，確認 GitHub Actions 與公開網址，再補記部署結果。

## Phase 6 公開完成紀錄（2026-08-31）
- 發布：提交 `b1de751` 已推送 `main`；GitHub Actions 工作流程 `33386345160` 的建置與 GitHub Pages 部署均成功。
- 公開網址：https://litostswirrl.github.io/six-counties/?rev=b1de751
- 公開驗證：390px 與 1200px 均確認有兩個 SVG 天際線，`viewBox` 為 `0 0 1440 300`；桌面高度 250px、手機高度 81.25px；首頁日間版使用透明背景、頁尾夜間版位於紫色背景上；點陣圖天際線數量為 0，沒有水平溢出。
- 後續：等待 Joseph 指定下一個明確範圍；正式團體資料到位後，人工確認數量與完整名稱。

## Phase 7 圖檔天際線背景與比例修正（2026-08-31）
- What：依 Joseph 最新確認，改回使用圖檔天際線，不再以 SVG 元件作為首頁與頁尾的實際素材。以影像生成工具重製目前已確認的地標構圖，保留由北至南的配置、少量光電板、水塔與一般屋頂；日間與夜間素材均處理為透明背景，並輸出為 `1440×300` 的 WebP。
- Why：目前視覺問題集中在圖檔如何融入區塊背景，以及天際線橫幅長寬比例；圖檔較能保留原先插畫的細節與整體風格。日間版可透出首頁米色背景，夜間版可透出頁尾深紫色，並移除大面積天空與上半部裁切。
- 驗證：`npm run test` 38 項通過；`npm run build` 通過，僅有既有字型路徑警告；`git diff --check` 通過；本機 1200px 與 390px 檢查確認首頁與頁尾各載入一張圖檔、圖片尺寸分別為 `1200×250` 與 `390×81.25`、沒有 SVG 或水平溢出。
- 發布：提交 `4fc8b80` 已推送至 `main`；GitHub Actions 工作流程 `33389459375` 的測試、建置與 GitHub Pages 部署均成功。
- 公開驗證：公開網址 `https://litostswirrl.github.io/six-counties/?rev=4fc8b80` 的 1200px 與 390px 畫面均確認兩張圖檔完整載入，圖片尺寸分別為 `1200×250` 與 `390×81.25`，首頁與頁尾背景色分別為米色與深紫色，沒有 SVG 或水平溢出。
- Next：等待 Joseph 指定下一個明確範圍；正式團體資料到位後，人工確認數量與完整名稱。

## Phase 8 天際線建物比例第二次修正（2026-08-31）
- What：以目前公開版圖檔作為母版，再以影像生成工具延展橫向構圖，保留既有地標、北到南順序、屋頂元素與水岸；移除生成圖的上下假背景留白後，重新輸出日間與夜間 `1440×300` 透明 WebP。
- Why：前一版為了符合 `1440×300` 而將約 `3：1` 的生成圖直接壓成 `4.8：1`，造成建物相對偏矮。本次先恢復生成圖的建物高度，再整理成相同的網站橫幅尺寸，降低比例變形。
- 驗證：`npm run test` 38 項通過；`npm run build` 通過，僅有既有字型路徑警告；`git diff --check` 通過；本機 1200px 與 390px 檢查確認兩張圖完整載入、顯示高度分別為 250px 與 81.25px，沒有 SVG 或水平溢出。
- 發布：提交 `86ff71f` 已推送至 `main`；GitHub Actions 工作流程 `33390543855` 的測試、建置與 GitHub Pages 部署均成功。
- 公開驗證：公開網址 `https://litostswirrl.github.io/six-counties/?rev=86ff71f` 的 1200px 與 390px 畫面均確認兩張圖完整載入，顯示高度分別為 250px 與 81.25px，沒有水平溢出；首頁米色與頁尾深紫色均正確透出透明背景。
- Next：等待 Joseph 指定下一個明確範圍；正式團體資料到位後，人工確認數量與完整名稱。

## Phase 9 天際線建築高度第三次修正（2026-08-31）
- What：沿用目前生成母版與地標配置，進一步收緊上下裁切範圍，讓建築主體填滿 `1440×300` 橫幅；同步縮減水面比例，保留細水岸線與透明背景。
- Why：Phase 8 處理後仍保留較多上下空間，建物高度的改善不夠明顯；本次直接減少底部水面與上方留白，讓建物比例在固定區塊高度內確實變高。
- 驗證：`npm run test` 38 項通過；`npm run build` 通過，僅有既有字型路徑警告；`git diff --check` 通過；本機 1200px 與 390px 檢查確認兩張圖完整載入、顯示高度分別為 250px 與 81.25px，沒有 SVG 或水平溢出。
- 發布：提交 `422734c` 已推送至 `main`；GitHub Actions 工作流程 `33391765842` 的測試、建置與 GitHub Pages 部署均成功。
- 公開驗證：公開網址 `https://litostswirrl.github.io/six-counties/?rev=422734c` 的 1200px 與 390px 畫面均確認兩張圖完整載入，顯示高度分別為 250px 與 81.25px，沒有水平溢出；首頁米色與頁尾深紫色均正確透出透明背景。
- Next：等待 Joseph 指定下一個明確範圍；正式團體資料到位後，人工確認數量與完整名稱。

## Phase 10 天際線公開版視覺比對與臺南地標修正（本機驗證完成，待公開部署，2026-09-01）
- What：將目前公開版 `3432728` 的日間與夜間天際線作為比對基準，確認這次整張重生成版本的建築確實較鬆、細節較軟，且赤崁樓的磚紅色過於突出。保留公開版整體構圖、北到南順序、建築尺度、屋頂元素與清晰度，只以影像生成結果替換臺南段的歷史建築，並將日間與夜間的臺南地標色彩各自調低飽和度。
- Why：使用相同尺寸、相同背景的並排視覺比對後，問題不只是測試視窗捲動位置；整張重生成圖在整理成網站固定橫幅時，建築高度優勢消失，且全圖細節受到重新繪製影響。局部替換能保留目前公開版已驗證的比例與畫質，同時讓臺南有可辨識的赤崁樓示例。
- 驗證：`npm run test` 38 項通過；`npm run build` 通過，僅有既有字型路徑警告；`git diff --check` 通過；本機 1200px 與 390px 檢查確認日間、夜間圖片均為 `1440×300` 原始尺寸，桌面為 `1200×250`、手機為 `390×81.25`，沒有水平溢出。
- Next：提交並推送 `main`，確認 GitHub Actions 與公開網址，再以公開網址重新比對桌面與手機畫面。

## Phase 10 公開完成紀錄（2026-09-01）
- 發布：提交 `df0c662` 已推送至 `main`；GitHub Actions 工作流程 `33470783780` 的建置與 GitHub Pages 部署均成功。
- 公開網址：`https://litostswirrl.github.io/six-counties/?rev=df0c662`。
- 公開驗證：以公開網址讀回日間與夜間圖檔，兩張圖片均完整載入，原始尺寸為 `1440×300`，桌面顯示為 `1200×250`、手機顯示為 `390×81.25`；首頁背景為米色、頁尾背景為深紫色，沒有水平溢出。
- 檔案核對：公開日間圖檔與本機檔案雜湊均為 `f5145843cfa9c8e86d1852b72dee9a67a80b182315f67790452a499237e63b5a`；公開夜間圖檔與本機檔案雜湊均為 `b8610a1bc7b69a6562f21e07a281cce79bdf246bf47f6b3b85d0c7b7f32ece54`。
- 後續：等待 Joseph 指定下一個明確範圍；正式團體資料到位後，人工確認數量與完整名稱。

## Phase 11 天際線基線連續性修正（2026-09-01）
- What：保留 Phase 10 的赤崁樓、建築尺度與整體配色，重新處理日間與夜間圖檔的合成底端；以目前公開版的原始基線恢復赤崁樓下方被前景層覆蓋的水平線。
- Why：前一版局部替換時，生成圖前景層覆蓋了原本連續的最底端水平線，導致整幅圖在赤崁樓位置出現斷點。本次只修復基線，不再改動地標或裁切範圍。
- 驗證：`npm run test` 38 項通過；`npm run build` 通過，僅有既有字型路徑警告；`git diff --check` 通過；本機 1200px 與 390px 檢查確認兩張圖完整載入，圖片尺寸為 `1440×300`，沒有水平溢出，日間與夜間底端水平線均連續。
- 發布：提交 `ce9a218` 已推送至 `main`；GitHub Actions 工作流程 `33473952257` 的建置與 GitHub Pages 部署均成功。
- 公開網址：`https://litostswirrl.github.io/six-counties/?rev=ce9a218`。
- 公開驗證：公開網址在 1200px 與 390px 均確認兩張圖完整載入，尺寸分別為 `1200×250` 與 `390×81.25`，沒有水平溢出；公開截圖確認赤崁樓下方的底端水平線已與左右兩側連續。
- 後續：等待 Joseph 指定下一個明確範圍；正式團體資料到位後，人工確認數量與完整名稱。

## Phase 12 天際線完整重生成（2026-09-01）
- What：放棄前一階段以舊圖加上局部臺南前景的合成方式，重新生成完整的日間與夜間天際線；兩張圖均從樹叢、水岸、建築到最底端水平線一起生成，再整理為透明背景的 `1440×300` WebP。
- Why：局部合成雖然修正了赤崁樓辨識度，卻讓樹叢、水岸與舊圖元素難以銜接，並造成底線斷裂。這次以完整重生成的單一畫面處理，避免保留舊圖像素或局部補丁。
- What：日間與夜間均採由北到南的地標配置：臺北 101、桃園機場塔台、臺中國家歌劇院、臺南赤崁樓、高雄 85 大樓與港區起重機；屋頂元素維持少量光電板、水塔與一般屋頂的平衡。
- 驗證：已檢查新圖預覽，兩張圖的基線從左到右連續，赤崁樓下方沒有斷點，圖片保留透明背景；`npm run test` 38 項通過；`npm run build` 通過，僅有既有字型路徑警告；`git diff --check` 通過；本機 1200px 與 390px 檢查確認兩張圖完整載入，原始尺寸為 `1440×300`、實際顯示高度分別為 250px 與 81.25px，沒有水平溢出。
- 發布：提交 `bbef9a8` 已推送至 `main`；GitHub Actions 工作流程 `33474850943` 的建置與 GitHub Pages 部署均成功。
- 公開網址：`https://litostswirrl.github.io/six-counties/?rev=bbef9a8`。
- 公開驗證：公開網址在 1200px 與 390px 均確認兩張圖完整載入，原始尺寸為 `1440×300`，實際顯示高度分別為 250px 與 81.25px，沒有水平溢出；首頁背景為米色、頁尾背景為深紫色；公開日間與夜間檔案分別與本機檔案雜湊一致。
- Next：等待 Joseph 指定下一個明確範圍；正式團體資料到位後，人工確認數量與完整名稱。

## 待決問題（提問清單草稿）
- 部署位置與網域、與淨零觀測站的關係
- 簽署／連署資料更新機制（10月記者會前後要更新，靜態 rebuild 或接 Google Sheet）
- 加入連署：站內表單或外連 Google Form；連署對象（團體／個人）
- 設計基調：延續淨零觀測站 vs 排碳大戶觀測站 vs 主視覺圖的米色＋紫綠
- 六都重要性數據段的呈現：資訊圖表互動程度

## Phase 13 投票箱、氣候風險與五大訴求圖示重繪（本機驗證完成，待公開部署，2026-09-01）
- What：重繪頂部投票箱、七種氣候風險圖示與五大訴求圖示；統一深色輪廓、圓角線條、色塊與視覺重心，補齊太陽光芒、降雨、海平面上升等必要部件。
- Why：目前畫面上的粗糙感集中在圖示家族不一致、細節密度過高與構圖未收乾淨；本次保留既有網站的米色、紫色、綠色與藍色視覺語言，改以簡化且可縮小閱讀的向量構圖處理。圖形生成僅作為先前方向評估，不新增生成圖檔或外部圖庫。
- 驗證：新增 `src/components/iconArtwork.test.tsx`，先確認 7 項測試在舊圖示上失敗，再以新圖示通過；`npm test` 45 項通過；`npm run build` 通過，僅有既有字型路徑警告；`git diff --check` 通過；本機 1200px 與 390px 瀏覽器檢查確認投票箱、風險矩陣與五大訴求圖示均載入，手機無水平溢出。
- 修改範圍：`src/components/BallotBox.tsx`、`src/components/PillarIcon.tsx`、`src/components/RiskMatrix.tsx` 與圖示完整性測試；未修改網站資料與文案。
- Next：提交並推送 `main`，確認 GitHub Actions 與 GitHub Pages 部署，再以公開網址重新檢查 1200px 與 390px 畫面。

## Phase 13 公開完成紀錄（2026-09-01）
- 發布：提交 `23f9e25` 已合併並推送至 `main`；GitHub Actions 工作流程 `33476398937` 的建置與 GitHub Pages 部署均成功。
- 公開網址：`https://litostswirrl.github.io/six-counties/?rev=23f9e25`。
- 公開驗證：1200px 與 390px 均確認投票箱、七種氣候風險圖示與五大訴求圖示完整載入；公開頁面無水平溢出，首頁投票箱與五大訴求圖示在手機尺寸下仍保持清楚輪廓。
- 後續：等待 Joseph 指定下一個明確範圍；正式團體資料到位後，人工確認數量與完整名稱。

## Phase 14 圖示逐顆視覺平衡修整（本機驗證完成，待公開部署，2026-09-01）
- What：逐一檢查投票箱、七種氣候風險與五大訴求圖示的視覺重心、線寬、留白、色塊比例與縮小後辨識度；修整投票箱紙張與箱體比例、能源圖的斜面重心、房子與愛心的疊合、太陽與溫度計的比例、淹水波浪間距、交通箭頭位置，以及乾旱圖的乾裂地面構圖。
- Why：上一版雖已補齊必要元素，但部分圖示仍有局部偏重、部件分離或概念不夠明確的問題；本次維持同一套圓角線條與米色、紫色、綠色、藍色配色，逐顆改善協調性。
- 驗證：`npm run test -- --exclude '.worktrees/**'` 45 項通過；`npm run build` 通過，僅有既有字型路徑警告；`git diff --check` 通過；本機 1200px 與 390px 瀏覽器檢查確認所有圖示在實際頁面中載入，手機無水平溢出。
- 修改範圍：`src/components/BallotBox.tsx`、`src/components/PillarIcon.tsx`、`src/components/RiskMatrix.tsx`；未修改網站資料、文案與版面結構。
- Next：提交並推送 `main`，確認 GitHub Actions 與 GitHub Pages 部署，再以公開網址重新檢查 1200px 與 390px 畫面。

## Phase 14 公開完成紀錄（2026-09-01）
- 發布：提交 `9a275c7` 已推送至 `main`；GitHub Actions 工作流程 `33477711830` 的測試、建置與 GitHub Pages 部署均成功。
- 公開網址：`https://litostswirrl.github.io/six-counties/?rev=9a275c7`。
- 公開驗證：以公開網址重新檢查 1200px 與 390px；投票箱、七種氣候風險與五大訴求圖示均實際載入，手機畫面沒有水平溢出。
- 已知警告：工作流程保留既有 Node.js 20 動作棄用提示與字型路徑警告，均未阻擋建置或部署。

## Phase 15 首頁團體名單入口調整（本機驗證完成，待決定是否公開部署，2026-09-01）
- What：移除首頁第三個盒子的團體名稱串列，改為「團體名單／查看完整名單」入口；桌面版在團體連署數量與入口之間加入細線右箭頭，手機版入口橫跨兩欄；入口導向既有的完整團體名單段落。
- Why：團體數量與團體名稱屬於不同資訊層級，首頁直接列出多筆名稱會造成統計盒子高度與閱讀重點失衡；完整名單已有固定呈現位置，首頁只保留清楚導引即可。
- 修改範圍：`src/App.tsx`、`src/sections/Hero.tsx`、`src/content/site.ts` 與 `src/content/site.test.ts`；資料來源、數量計算與下方團體名單未修改。
- 驗證：先確認新增測試在舊程式上失敗，再加入實作後 `npm test` 46 項通過；`npm run build` 通過；`git diff --check` 通過；本機桌面與 390px 手機畫面確認箭頭、完整名單入口與手機橫跨兩欄版面；點擊入口後網址為 `#endorse` 且定位到完整名單；鍵盤聚焦外框可見；桌面與手機均無水平溢出。
- 已知瀏覽器訊息：本機頁面仍有既有 `/favicon.ico` 404，與本次調整無關；建置仍有既有字型路徑警告。
- Next：若確認視覺方向，可提交並推送 `main`，再驗證 GitHub Actions、公開網址與桌面／手機畫面。

## Phase 16 首頁統計與可捲動團體名單（本機驗證完成，待決定是否公開部署，2026-09-01）
- What：首頁恢復第三個「公民連署數量」統計盒；第二個「團體連署數量」盒右下角加入單行 `>> 查看完整名單`，連到下方名單區塊；底部連署團體改為可收合的完整名單盒，設定高度上限與縱向捲軸，支援大量團體名稱。
- Why：首頁三個盒子回到同一種統計層級，完整名單則集中在頁面底部，避免大量名稱破壞首屏閱讀；可收合與固定高度讓名單筆數增加時不會無限拉長頁面。
- 修改範圍：`src/App.tsx`、`src/sections/Hero.tsx`、`src/sections/Endorsements.tsx`、`src/content/site.ts`、`src/content/site.test.ts` 與 `src/sections/Endorsements.test.tsx`；移除底部重複的公民人數大卡，保留資料來源與狀態處理。
- 驗證：新增測試先在舊程式上失敗；目前目標測試 9 項通過，瀏覽器檢查確認手機連結單行靠右下、桌面三盒對齊、點擊首頁連結後自動展開底部名單、名單盒 `overflow-y: auto` 且有高度上限、底部按鈕可收合，桌面與手機均無水平溢出。
- Next：完成最後一次 `npm test`、`npm run build` 與 `git diff --check` 後，若確認視覺方向，再提交並推送 `main`。

## Phase 17 固定高度團體名單（公開完成，2026-09-01）
- What：依最新決定移除底部團體名單的展開／收合功能，改為頁面載入後直接顯示固定高度的完整名單區塊；區塊保留縱向捲軸，示意資料增加至十二個團體，以驗證超過六筆時的瀏覽方式。
- Why：單頁式頁面不需要再維護名單的開關狀態；固定高度可以讓使用者直接看到前六筆，也不會因團體數量增加而推長整個頁面。
- 修改範圍：`src/data/demo.ts`、`src/sections/Endorsements.tsx`、`src/sections/Endorsements.test.tsx` 與 `src/sections/Hero.tsx`、`src/sections/Hero.test.tsx`；同步更新示意數量測試。
- 驗證：先確認新測試在舊實作上失敗；目前 `npm test` 49 項通過，`npm run build` 通過，`git diff --check` 通過；本機桌面與手機瀏覽器確認三個統計標籤頂端對齊，第二個盒子的入口固定在右下角，名單區塊為 `304px` 可捲動高度、十二筆資料內容高度為 `329px`（手機內容高度為 `634px`），沒有 `<details>`、`<summary>` 或水平溢出，瀏覽器錯誤訊息為零。
- 發布：提交 `da8ca39` 已推送至 `main`；GitHub Actions 工作流程 `33480344675` 的建置與 GitHub Pages 部署均成功。
- 公開網址：`https://litostswirrl.github.io/six-counties/?rev=da8ca39`。
- 公開驗證：公開網址在桌面與手機均確認三個統計標籤頂端對齊，第二個盒子的入口固定在右下角；名單區塊為 `304px` 可捲動高度、十二筆資料內容高度分別為桌面 `329px` 與手機 `634px`，沒有 `<details>`、`<summary>` 或水平溢出。
- 已知訊息：公開頁面仍有既有 `/favicon.ico` 404；GitHub Actions 顯示既有 Node.js 20 動作棄用提示，均未阻擋建置或部署。

## Phase 19 日期驅動時程與連署意見區調整（本機驗證完成，尚未提交與部署，2026-09-01）
- What：行動時程改用臺灣日期判定目前節點，並在跨過臺灣午夜後自動重新讀取日期；保留原有發光／閃紫動畫和減少動態效果支援。連署團體名單盒改為 `max-h-56`，讓手機最後一列只露出約一半並保留捲軸；新增「公民連署意見」標題，跑馬燈改為 22.5 秒一輪，示意意見增加至六則。
- Why：回應使用者希望高亮項目隨日期自動調整，以及以名單截斷示意捲軸、補充意見區標題和樣板的需求；未改變既有動畫風格。
- 驗證：新增測試先在舊實作失敗，再完成修改；`npm test` 54 項通過；`npm run build` 通過，保留既有字型路徑警告；`git diff --check` 通過；本機 1200 像素與 390 像素均無水平溢出，名單盒可捲動，瀏覽器沒有錯誤或警告訊息。
- 狀態：本階段修改尚未提交、推送或部署；工作樹原有的 `tsconfig.tsbuildinfo` 與瀏覽器暫存紀錄仍未納入本次修改。
- Next：若確認本機視覺方向，再提交並推送 `main`，等待工作流程完成後，以公開網址重新檢查桌機與手機畫面。

## Phase 20 最後期限後的完成狀態（本機驗證完成，尚未提交與部署，2026-09-01）
- What：最後期限後的 `currentPhaseIndex` 改回傳完成狀態；2026年10月5日仍維持最後節點紫色高亮，2026年10月6日起五個節點全部轉為綠色，並移除「進行中」標籤。
- Why：釐清 2026年9月29日至10月5日高亮最後節點，10月6日後應呈現全部行動完成，而不是讓最後節點持續顯示紫色進行中。
- 驗證：新增截止後測試先在舊實作失敗，再完成修改；`npm test`、`npm run build`、`git diff --check` 通過；使用固定 2026年10月6日日期注入，於 1200 像素與 390 像素確認五個節點皆為綠色、沒有「進行中」、沒有水平溢出。
- 狀態：本階段修改尚未提交、推送或部署。
- Next：若確認本機視覺方向，再提交並推送 `main`，等待工作流程完成後，以公開網址重新檢查桌機與手機畫面。

## Phase 20 公開部署完成紀錄（2026-09-01）
- 發布：提交 `be5607a` 已推送至 `main`；GitHub Actions 工作流程 `33483382626` 的建置與 GitHub Pages 部署均成功。
- 公開網址：`https://litostswirrl.github.io/six-counties/?rev=be5607a`。
- 公開驗證：1200 像素與 390 像素均確認跑馬燈速度為 22.5 秒、六則公民連署意見形成 12 張循環卡片、「公民連署意見」標題存在、連署團體盒高度為 238 像素且可捲動；兩種尺寸均沒有水平溢出，瀏覽器沒有錯誤或警告訊息。
- 已知警告：工作流程有既有 Node.js 20 棄用提示；本機建置有既有字型路徑警告，均未阻擋部署。
- 後續：GitHub Pages 仍為目前公開環境；未來可評估遷移至 GCAA 官方 GitHub 組織或 Google Firebase 等方案，依轉移條件選擇。

## Phase 18 頂部統計盒垂直置中（公開完成，2026-09-01）
- What：將三個統計盒的標題與數量作為同一組垂直置中；桌面版把「>> 查看完整名單」放入團體數量列，與「12 個」同列對齊；手機版維持入口在第二盒右下角，避免窄版面重疊。
- Why：原本標題與數量貼近統計盒上方，第二盒入口又獨立貼在底部，三個盒子的內容結構不一致；讓標題與數量成組置中後，三個盒子的視覺重心一致。
- 修改範圍：`src/sections/Hero.tsx` 與 `src/sections/Hero.test.tsx`。
- 驗證：先確認新排版測試在舊實作上失敗；目前 `npm test` 49 項通過，`npm run build` 通過，`git diff --check` 通過；本機桌面確認三個標題頂端同為 `264.3125px`、三個數量列頂端同為 `289.8125px`，入口與團體數量列同列且無水平溢出；手機確認入口距離第二盒右側 `22.25px`、底部 `13.75px`，頁面沒有水平溢出。
- 發布：提交 `7df8b7f` 已推送至 `main`；GitHub Actions 工作流程 `33481025624` 的建置與 GitHub Pages 部署均成功。
- 公開網址：`https://litostswirrl.github.io/six-counties/?rev=7df8b7f`。
- 公開驗證：公開網址在桌面確認三個標題頂端與數量列頂端分別對齊，入口與「12 個」同列；手機確認入口仍位於第二盒右下角，兩種尺寸均沒有水平溢出。
- 已知訊息：公開頁面仍有既有 `/favicon.ico` 404；GitHub Actions 顯示既有 Node.js 20 動作棄用提示，均未阻擋建置或部署。

## Phase 21 全站審查修正批次＋連署與候選人資料串接（公開完成，2026-09-03）
- 目標：一次處理 Joseph 2026-09-03 提出的整批修正（選單命名、首頁標題與字體、六都現況文案與風險表、五大訴求版面、行動時程節點、候選人承諾標籤與圖示、連署團體真實名單與版面、區塊順序、頁尾文字），並把連署資料來源改接 Google 表單回覆試算表。
- 資料來源事實（2026-09-03 以工作帳號 gws 讀取）：表單回覆試算表 `1TvrN08uKSxLQkbN2SpcNvwVD-MupXiHAZpLRHonPdTs` 只有一個工作表「表單回覆 1」，24 欄；團體 4 筆（TRENA 台灣再生能源推動聯盟、荒野保護協會、台灣綠黨、臺南市台南新芽協會）、個人 6 筆。個人「同意公開」且有留言者 0 筆，因此跑馬燈維持示意意見。試算表未公開（匿名 401），網站不能直接用 gviz 讀，必須經 Apps Script 中介。
- 決策：
  - 團體名單改由 Apps Script 回傳（表單回覆 D 欄 ＋ 試算表可選的「團體補登」工作表 A 欄），gviz「團體連署」工作表路徑移除；候選人簽署仍走 gviz。
  - Apps Script 網址尚未部署前，網站顯示 `src/content/orgs.ts` 的 ENDORSING_GROUPS 六個真實團體（表單四個加 Joseph 指定的野薑花公民協會、綠色和平；名稱用通用簡稱：台灣再生能源推動聯盟、台南新芽協會）。
  - 個人留言只在「同意公開」且留言非空時進跑馬燈；姓名依「希望公開哪些資訊」欄位：選公開姓名者遮第二字顯示，其餘顯示「連署公民」。
  - 標題字體統一為內文字體：改 `--font-display` token 指向 Noto Sans TC，並移除 jf-openhuninn 的 @font-face；`public/fonts/jf-openhuninn-2.1.woff2` 不再被引用。
  - 「已拜會」狀態整個移除（型別、解析、示意資料、文件）；候選人標籤改為 全數承諾／部分承諾 n 項／尚未回應。
  - 高溫專區（HeatExplorer）與縣市高溫資料表（COUNTY_HEAT 等）一併移除，heatData 只留風險欄位定義。
  - 行動時程：移除 9/11 節點，新增無日期的「選後持續監督」；無日期節點視為永遠可為目前節點，10/6 起紫色高亮停在該節點，不再有全綠完成狀態。
  - 三個統計數字說明文字與風險表說明文字經比對已與 Joseph 給的版本相同，不需修改。
- 已知取捨（待 Joseph 確認）：關於我們與公民社會連署響應相鄰且都列九個發起團體，內容重複。
- 分工：Opus 工作者 A 負責圖示（簽名手勢、腳踏車、水災或強降雨合併表頭圖示、圖示測試）；Opus 工作者 B 負責 Apps Script 與文件；其餘由主迴圈處理。
- 驗證：`tsc --noEmit` 通過；`vitest` 9 個檔案 61 項通過；`npm run build` 通過（字型路徑警告已隨 @font-face 移除消失）；`git diff --check` 通過。本機 `vite preview` 以 1200px 檢查：選單六項順序、h1 含「六都市長候選人」與簽名圖示、字體為 Noto Sans TC、統計盒兩個且各有入口、風險表五欄與備註、五大訴求編號標題與腳踏車圖示、時程五節點且目前停在 9/18、候選人標籤三種與空心圓、發起團體與連署團體各為三欄格狀、頁尾三行；區塊順序 top → why-six → demands → timeline → board → about → endorse → join。手機檢查：瀏覽器可縮到的最小寬度為 500px，無水平溢出，風險表在自身容器內橫向捲動。主控台只有既有 `/favicon.ico` 404。
- 交接：Apps Script 尚未部署，`src/data/config.ts` 的 `APPS_SCRIPT_URL` 仍為空；部署步驟見 `docs/apps-script-deploy.md`（Code.gs 已改為讀表單回覆試算表，SHEET_ID 要填表單回覆試算表的編號 `1TvrN08uKSxLQkbN2SpcNvwVD-MupXiHAZpLRHonPdTs`）。`public/fonts/jf-openhuninn-2.1.woff2` 已無程式引用，可刪。`src/data/petition.ts` 的 `validatePetition`／`submitPetition` 是站內表單時代留下的既有未使用程式，未動。
- Next：Joseph 確認視覺方向後提交並推送 `main`，等 GitHub Actions 完成再以公開網址複查桌機與手機。
- 2026-09-03 第二輪（Joseph 回覆後）：公民社會連署響應移除發起團體格；「公民連署意見」改為「連署公民」，右側顯示「共 n 位」，跑馬燈維持公開意見。表單回覆試算表已有「團體補登」分頁（五筆、無標題列，與表單有三筆重複），Apps Script 改為：補登分頁標題列可有可無；表單名單與補登名單合併時忽略空白與臺／台差異、名稱互相包含即視為同一團體並以補登寫法顯示；本機以 node 驗證合併結果為六個團體。網站內建名單同步為 台灣再生能源推動聯盟、荒野保護協會、台灣綠黨、台南新芽、綠色和平、野薑花公民協會，連署公民人數快照 7（2026-09-03 表單個人回覆數）；Apps Script 部署後改為即時值。本機 dev server：http://localhost:5173/six-counties/
- 2026-09-03 Apps Script 已由 Joseph 部署，網址填入 `src/data/config.ts` 的 `APPS_SCRIPT_URL`。匿名 curl 與瀏覽器（dev server）都確認抓到即時資料：7 位、6 個團體、1 則公開意見（顯示為「連署公民」），主控台無錯誤，代表跨網域讀取正常。`config.ts` 兩個常數改為明確 `string` 型別，避免字面值型別讓「是否為空」的比較被 TypeScript 判為永遠不成立。`petition.ts` 拆出 `FALLBACK_PETITION_STATS` 與 `parsePetitionResponse`，測試不再碰網路；62 項測試通過。目前只有一則真實公開意見，跑馬燈會重複同一張卡片，這是資料狀態不是錯誤。候選人簽署試算表仍待提供（`SHEET_ID` 留空，看板維持示意資料）。尚未提交。
- 2026-09-03 候選人簽署試算表由我以工作帳號建立：`18MZP1jPFbK7Orn_W-66r-LjVsIpFIt1GkYwSlfvS_GY`（工作表「候選人簽署」，24 欄標題列、縣市與狀態下拉、訴求 1-18 勾選框、標題列凍結）。已設「知道連結的任何人可檢視」，匿名 gviz 讀取確認 status ok；`SHEET_ID` 已填入 `config.ts`。勾選框回傳 TRUE，`parseCheck` 原本就接受。目前無資料列，網站看板應顯示「名單整理中」空狀態。文件步驟一改為指向既有試算表。
- 瀏覽器確認：試算表空表時看板顯示「名單整理中」、無示意標記、無主控台錯誤。首頁候選人數在空表狀態原本顯示「—」，改為 0（`App.tsx` 把 empty 也視為已讀到資料）。

## Phase 21 公開完成紀錄（2026-09-03）
- 發布：提交 `3a60ff1` 已推送至 `main`；GitHub Actions 工作流程 `33717177600` 的測試、建置與 GitHub Pages 部署均成功；公開網址的 JS 檔名與本機 `dist/index.html` 一致。
- 公開網址：`https://litostswirrl.github.io/six-counties/?rev=3a60ff1`。
- 公開驗證（1200px）：字體為 Noto Sans TC；選單六項與區塊順序正確；首頁候選人 0 位、團體 6 個；看板顯示「名單整理中」且無示意標記；連署團體六個、連署公民共 7 位、跑馬燈一則真實意見；風險表五欄；時程末節點「選後持續監督」；無水平溢出；主控台僅既有 `/favicon.ico` 404。
- 後續待辦：候選人簽署試算表待同事填寫；同意公開的意見增加後跑馬燈自然填滿；`public/fonts/jf-openhuninn-2.1.woff2` 已無引用可刪。

## Phase 22 第三輪審查修正：狀態標籤、日期、跑馬燈、文案斷行、圖示重繪（進行中，2026-09-03）
- 目標：處理 Joseph 2026-09-03 第三輪回饋：候選人狀態標籤誤顯示全數承諾、簽署日期原始格式外露、連署公民跑馬燈過慢、加入連署與五大訴求說明斷行、首頁「六都市長候選人」納入紫色漸層並與主標同字級、圖示逐顆視覺檢查與重繪（簽名手勢、太陽能板格線、水災或強降雨合併圖示）。
- 根因：
  - 狀態標籤只看試算表「狀態」欄，測試列「已簽署」但只勾 15 項仍顯示全數承諾。改為由勾選數推導：18 項全勾為全數承諾，1 到 17 項為部分承諾 n 項，0 項或狀態欄空白為尚未回應。
  - gviz 日期欄回傳 `Date(2026,8,25)`（月份從 0 起算），格式化字串在 `f` 欄。解析時對 `Date(` 開頭的值改用 `f`，網站顯示改為「2026年9月25日」。
  - 跑馬燈動畫固定 22.5 秒跑完一輪，卡片少時位移距離短，速度就慢；只有一則真實意見時約 12px/s，六則示意時約 72px/s。改為依卡片數換算時間（每張 3.75 秒），並把基底卡片補到至少六張，速度固定。
- 圖示檢查方式：以 vite-node 把三個圖示元件渲染成靜態 HTML（scratchpad/icons.html，200px 與 32px 並排），用 chrome-devtools 截圖逐顆看。
- 圖示決策：
  - 簽名手勢整個重畫：筆改用 rotate(45) 的區域座標繪製，手為握拳（掌、兩個指節弧線）加食指沿筆桿、拇指壓筆，三個版本截圖比較後採用緊湊版。
  - 太陽能板改為真正的平行四邊形，格線由邊向量內插計算（三欄兩列），不再歪斜或超出板面；支架改置底邊中點。
  - 腳踏車改為標準菱形車架（綠色）、齒盤小圓、座墊與把手線條對齊；韌性圖的窗戶移到門左側，不再被愛心遮住。
  - 「水災或強降雨」表頭不再用合併圖示，改為並排顯示淹水（房子＋兩道水波）與強降雨（雲＋三滴雨）兩顆既有圖示，與儲存格內的圖示完全一致；HazardIcon 移除 water 變體與 HazardIconKey 型別。
  - 乾旱、海平面上升、坡地、強風、高溫、水患治理等圖示經 200px 與 32px 檢視後未改。
- 驗證：`tsc --noEmit` 通過；vitest 10 個檔案 65 項通過（新增 StatusBadge 測試、gviz 日期測試、formatSignedDate 測試、風險表表頭測試）；`npm run build` 通過；`git diff --check` 通過。本機 dev server 1400px：首頁三行標題同字級同漸層、簽名圖示以 0.95em 隨標題縮放；候選人看板兩筆測試列顯示「部分承諾 15 項」「部分承諾 1 項」、簽署日期「2026年9月25日」；跑馬燈 12 張卡片 22.5 秒；加入連署與五大訴求說明各兩行；風險表表頭兩顆圖示並排。500px：無水平溢出、標題三行正常。主控台無錯誤與警告。
- 已知事項：首頁「候選人簽署共 2 位」把「部分簽署」也算進去（App.tsx 既有邏輯，本輪未動）；試算表的「狀態」欄現在只決定「尚未回應」，全數／部分由勾選數判定，同事填表時只勾訴求即可。
- Joseph 追加回饋：淹水、海平面上升與水患治理圖示的最底一道水波被裁到。原因是波谷加上線寬超出 viewBox 底邊（28 格的圖示波浪原在 y=26，波谷 27.75 加線寬到 28.75）。三組波浪各上移 1.5 格，200px 預覽確認完整；測試、建置與格式檢查重跑通過。
- Joseph 第二次追加：乾旱圖示的太陽改用與能源圖相同的八道光芒構圖（比例照能源圖縮放）；淹水圖示的房子上移 2 格，與水波不再重疊，綠色填色改為畫在屋頂線下方的五邊形，不再高出牆線；坡地圖示的三條陰影線改為等長、等距、與右側山面平行。預覽截圖確認後，tsc、vitest 65 項、build、diff --check 重跑通過。
- 狀態：尚未提交、推送或部署。

## Phase 22 公開完成紀錄（2026-09-03）
- 發布：提交 `0e89fd0` 已推送至 `main`；GitHub Actions 工作流程 `33719918273` 的建置與 GitHub Pages 部署均成功；公開網址的 JS 檔名與本機 `dist/index.html` 一致。
- 公開網址：`https://litostswirrl.github.io/six-counties/?rev=0e89fd0`。
- 公開驗證（1400px）：首頁三行標題同為 68px 且都套漸層；候選人看板兩筆測試列顯示「部分承諾 15 項」「部分承諾 1 項」，簽署日期為「2026年9月25日」「2026年9月22日」；跑馬燈 12 張卡片 22.5 秒；加入連署與五大訴求說明各兩行；風險表表頭第一欄兩顆圖示、其餘各一顆；新版簽名手勢已載入；無水平溢出。500px：無水平溢出，標題 37.4px。主控台無錯誤與警告（既有 favicon 404 未再出現）。
- 後續待辦：首頁「候選人簽署共」目前把「部分簽署」也算進去，待 Joseph 決定是否只算全數承諾；候選人簽署試算表的兩筆測試列上線前要清掉。

## Phase 23 中英數之間統一加空格（2026-09-03）
- What：以公開頁面 `document.body.innerText` 掃描中文與英數相鄰的位置，只剩十處，全部是數字與中文相鄰：首頁「2026年」、六都現況「2050淨零碳排」、訴求「2035年」「800kW以上」、時程四個日期、簽署日期格式。全部改為加空格，日期一併套用（8 月 12 日、2026 年 9 月 25 日），單位與數字之間不加（800kW）。
- 修改範圍：`src/content/site.ts`、`stats.ts`、`demands.ts`、`timeline.ts`、`src/data/demo.ts`、`src/data/sheets.ts` 的 `formatSignedDate` 與對應測試。
- 取捨：日期是否加空格在台灣沒有單一慣例（新聞與公文多不加，技術寫作多加）；為了與站內已有的「共 7 位」「18 項」一致，選擇全站統一加。若 Joseph 偏好日期不加，只需回復 timeline.ts、site.ts、demo.ts 與 formatSignedDate。

## Phase 24 簽名圖示改為平面風格（2026-09-03）
- What：依 Joseph 提供的參考圖重畫首頁簽名圖示：無外框平面色塊，手掌在中、手腕朝右、三指在左下、拇指壓筆，筆呈 45 度、右側一條直立短線。配色改用主視覺色系：手掌淡紫 #D9C4DC、拇指 #B892BC、筆身天空藍、筆蓋與筆尖深紫、筆環藍綠、直線綠。
- 取捨：參考圖的膚色與橘色不在主視覺色系內，改用紫色系手掌與綠色直線；手掌用淡紫是為了在米色背景與紫色漸層標題之間仍有區隔。圖示測試改為檢查 civic-flat 家族、手／拇指／筆／線四個部件且無 stroke。
- 發布：提交 `030b624` 已推送至 `main`，GitHub Actions 建置與部署成功，公開網址 `https://litostswirrl.github.io/six-counties/?rev=030b624` 的 JS 檔名與本機一致；公開頁面首頁圖示為 civic-flat 家族、四個部件、無 stroke，顯示尺寸約 65px。

## Phase 25 風險表說明改寫、簽名圖示恢復參考圖配色（2026-09-03）
- What：風險表「六都面對哪些氣候風險」下方說明改為 Joseph 給的版本：「六都面對高溫、水災或強降雨、乾旱、海平面上升、坡地・土砂、強風等氣候風險，而高溫調適和水患是六都的共同議題。」（`src/components/RiskMatrix.tsx`）。Joseph 同時貼來的「五大訴求，十八項政策承諾」標題與說明文字，與 `site.ts` 現有內容逐字相同，未動。
- 簽名圖示：Phase 24 的紫色系配色不採用，改回參考圖顏色。以 PIL 取參考圖主要色值：手掌 #F6D2D1、拇指 #EEADAC、筆身 #93D6F3、筆環 #78A7F8、筆蓋與筆尖 #4A5688、直線 #ED6E35。不再引用主視覺 CSS 變數。
- Joseph 追加：形狀也要更像參考圖。把參考圖（110×112px）逐列分色印成字元圖，量出各部件位置後，viewBox 改為參考圖座標「3 1 110 110」直接重畫：筆以筆尖 (13.5, 91) 為原點旋轉 33.6 度，筆身分淺藍（左上半）與中藍（右下半）兩色、筆尖收尖、藏青筆蓋；沿筆桿左上側一根深粉色手指（頂端貼近筆蓋、指尖在筆尖上方）；手掌從筆桿中線往右延伸，上緣在筆右側隆起後緩降到手腕，下緣圓弧；掌下兩根捲曲手指改為與掌相連的圓角矩形；橘色直條 x 95.5、高 46。與參考圖並排截圖確認 200px、64px、32px 三種尺寸。
- 驗證：`tsc --noEmit`、vitest 10 檔 65 項、`npm run build`、`git diff --check` 通過；scratchpad/icon.html 以 chrome-devtools 截圖，200px、64px、32px 與參考圖並排比對，配色一致。
- 發布：提交 `0e94f0a`（文字與配色）與 `9abe10e`（形狀重畫）已推送至 `main`，GitHub Actions 工作流程 `33721474172`、`33721750309` 建置與部署均成功。公開網址 `https://litostswirrl.github.io/six-counties/?rev=9abe10e` 的 JS 檔名 `index-BllkoFoo.js` 與本機 `dist/index.html` 一致；下載該 JS 確認含新版風險表說明、civic-flat 家族、參考圖六個色值與新 viewBox。

## Phase 26 訴求區標題改名（2026-09-03）
- What：`SITE.sections.demands.title` 由「五大訴求，十八項政策承諾」改為「五大面向，十八項政策訴求」，對應測試同步更新。導覽列的「五大訴求」與首頁第三行「九個公民團體提出五大訴求，十八項政策承諾」Joseph 未點名，未動；若要全站一致，這兩處待他決定。
- 發布：提交 `2482e50` 已推送，GitHub Actions 工作流程 `33727429857` 成功；公開網址 `?rev=2482e50` 的 JS 檔名 `index-D8DxqX9n.js` 與本機一致，內含新標題一次，舊字串僅剩首頁那一行。
- Joseph 更正：標題改回「五大訴求，十八項政策承諾」，與導覽列及首頁第三行一致；Phase 26 的改名作廢。

## Phase 27 第四輪修正：noindex、首頁副標、底色、配色比較、六都現況文案、高溫欄、連署區標題、團體與意見來源（進行中，2026-09-04）
- 目標：Joseph 2026-09-04 十項回饋。(1) 網站不被搜尋引擎索引但有連結仍可看；(2) 首頁「六都市長候選人」改用與標語「面對城市的下一個十年…」相同字體設定，簽名圖示隨之縮小；(3) 米色底 #EEEBE2 提亮為 #FDF9F2，其他略深底色一併提亮；(4) 盤點全站配色（含圖片與圖示），把紫色換成幾組橘色系並截圖首頁給同事比較；(5) 確認天際線圖片是否去背；(6) 六都現況三格文字換成 Joseph 提供版本；(7) 風險表放回高溫欄；(8) 「連署公民」改「公民連署意見」並拿掉「共 n 位」；(9) 連署團體要同時計入「表單回覆 1」D 欄有團體名稱的列與「團體補登」；(10) 團體列若 J 欄有連署理由、L 欄同意公開，也要進跑馬燈，名字取 F 欄。
- 盤點結果：
  - 天際線兩張 webp 都是 RGBA，透明像素約 63%，最上與最下列全透明，沒有烘進底色，不需重做。圖片本身沒有紫色像素（主色是深藍、藍綠、綠與米色）。
  - 紫色只出現在 CSS 變數 purple-deep／mid／light、`.gradient-title` 寫死的三個色碼、PillarIcon 的愛心與兩個圓、交通圖示的箭頭、Timeline 進行中節點、按鈕與頁尾。簽名圖示用參考圖固定色（粉紅手、藍筆、橘線 #ED6E35），不隨主題變。`Skyline.tsx` 是既有未使用的元件（首頁用 webp），未動。
  - Apps Script 線上輸出已含「台灣再生能源推動聯盟」與「台灣環境保護聯盟」共七個團體，但網站內建備援名單只有六個（缺環盟）。表單回覆試算表不公開、gws 授權過期（401），無法讀原始列驗證 Joseph 看到缺漏的原因；最可能是該列 C 欄不是「團體」或為空，被現行「C 欄必須是團體」的條件濾掉。
- 決策：
  - noindex 只用 `<meta name="robots" content="noindex">`，不加 robots.txt 的 Disallow：擋掉爬蟲反而讓它讀不到 noindex 標記，Google 文件明說兩者不要並用。
  - `.gradient-title` 改引用 CSS 變數，配色比較時只要覆寫三個變數即可整站換色（含 PillarIcon、Timeline、按鈕、頁尾）。
  - 高溫欄放回第一欄，六都皆有高溫（沿用 3a60ff1 之前的資料）；圖示沿用當時的太陽（填色 purple-light，會隨配色變）。
  - 連署區拿掉「共 n 位」後 `individualCount` 屬性無用途，從 Endorsements 與 App 移除；Apps Script 仍回傳 individualCount，首頁未用。
  - Apps Script：團體改為「D 欄非空即計入」，不再看 C 欄；團體意見取 J 欄，L 欄以「同意」開頭視為同意公開，名字取 F 欄並用與個人相同的遮罩（第二字改○），因為 F 欄推測是聯絡人姓名而非團體名。個人與團體意見合併後取最後 30 則。欄位對應（F/J/L）來自 Joseph 描述，未能開表核對。
  - 內建備援名單補上「台灣環境保護聯盟」，順序照線上輸出。
- 配色比較：dev server 注入 `<style>` 覆寫三個變數，chrome-devtools 以 1400×1000 截首頁，輸出到 `design/color-schemes/`。候選配色的深色都以白字對比 ≥ 4.5、中間色在米色底上對比 ≥ 4.5 為門檻。
- 驗證（2026-09-04）：`tsc --noEmit` 通過；vitest 10 檔 66 項通過（風險表測試改為六欄且六都皆含高溫、圖示測試加 heat、連署區測試改為「公民連署意見」且無人數、備援名單長度 7）；`npm run build` 通過；`git diff --check` 通過。dev server 1400px 檢查：`meta robots` 為 noindex；「六都市長候選人」與標語同為 25.5px、字重 400、墨色、無字距，簽名圖示約 31px；body 底色 rgb(253,249,242)；風險表表頭七欄（縣市＋高溫＋五欄）、六都高溫欄皆有圖示；連署區小標為「連署團體」「公民連署意見」，無「共 n 位」；六都現況三段文字為新版；加入連署區塊底色改為 purple-deep 3%。
- 配色比較輸出：`design/color-schemes/` 六張首頁截圖、`comparison.png` 並排圖與 `README.md`（色碼與對比值）。
- 未完成／待 Joseph：(a) 表單回覆試算表欄位 F／J／L 的對應未能開表核對，gws 需重新登入（`gws auth login`）後才能驗證；Apps Script 修改後要在 script.google.com 貼上新版 Code.gs 並以「管理部署作業→新版本」重新部署，網址不變。(b) 配色尚未定案，程式仍是紫色；選定後只改 `global.css` 三個變數。(c) 尚未提交。
- 2026-09-04 Joseph 重新登入 gws 並重新部署 Apps Script 後核對：表單回覆 1 的欄位為 C 身分、D 團體名稱、F 聯絡人、J 團體連署理由、L 團體是否同意公開；個人段為 N 姓名、T 理由、U 同意公開、V 公開哪些資訊，與程式假設一致。試算表 15 筆回覆中 5 筆團體（C 欄都是「團體」）、10 筆個人。新版線上輸出：個人 10、團體 7（含 TRENA 與環盟）、公開意見 5 則（新增荒野與環盟兩則團體理由，名字為聯絡人遮罩「林○蘭」「林○淵」）。之前看到缺漏應是網站備援名單（僅六個）而非試算表問題。

## Phase 27 公開完成紀錄（2026-09-04）
- 配色決定：暫留現行紫，候選只剩紫與琥珀；程式未改色。
- 發布：提交 `a9bcc05` 已推送至 `main`；GitHub Actions 工作流程 `33830241304` 成功（npm ci 步驟約十分鐘，較往常慢，屬 runner 狀況）。公開網址 `https://litostswirrl.github.io/six-counties/?rev=a9bcc05` 的 JS 檔名 `index-DfMriJJX.js` 與本機 `dist/index.html` 一致；HTML 含 `<meta name="robots" content="noindex">`；CSS 含 #fdf9f2；JS 含「公民連署意見」、三段新文案、「台灣環境保護聯盟」、高溫欄標籤與縮小後的簽名圖示尺寸，且不再含「連署公民」字串（Apps Script 回傳的匿名名字仍會顯示為連署公民，那是資料不是程式字串）。
- 後續待辦：琥珀若採用，改 `global.css` 三個變數並把中色加深到對比 4.5 以上；團體意見卡片的名字是否改顯示團體名，待 Joseph 決定。

## Phase 28 訴求區與簽署看板說明文字（2026-09-04）
- What：Joseph 提供新版文案。訴求區「五大訴求，十八項政策承諾」下方說明改為兩段（氣候風險升高／針對六都市長選舉提出五大政策訴求），`demandsLead` 由單一字串改為兩段陣列。原第二段「九個公民團體針對五大面向提出十八項政策訴求…」與新文案第二段重複，移除；「點開每個面向，看完整的政策內容。」是操作提示，保留。候選人區標題由「候選人承諾」改為「候選人簽署看板」，說明改為「我們邀請六都市長候選人回應五大訴求，以下是各候選人簽署政策承諾情形，供選民參考。」（新增 `boardLead`）。
- 未動：導覽列仍是四字的「候選人承諾」，Joseph 未點名；「九個公民團體」一詞在訴求區消失，首頁第三行仍有。
- 驗證：`tsc --noEmit`、vitest 10 檔 67 項、`npm run build`、`git diff --check` 通過。

## Phase 29 風險表備註改寫（2026-09-04）
- What：`RISK_MATRIX_NOTE` 改為 Joseph 給的版本：「本表呈現六都氣候變遷調適執行方案所評估的相對風險；未列出不代表該縣市不存在相關災害，仍需持續關注該項風險。」沒有測試引用此字串。
- 發布：提交 `34663ba` 已推送至 `main`，GitHub Actions 工作流程 `33850461878` 成功（build 步驟再次超過七分鐘）。公開網址 `?rev=34663ba` 的 JS 檔名 `index-BtDO1Y6g.js` 與本機一致，內含新備註。
- Joseph 再調整：備註改為「本表呈現六都各自在氣候變遷調適執行方案所評估的相對主要風險；未列出不代表該縣市不存在相關災害，仍需持續關注該項風險。」
- 發布：提交 `5edbdff` 已推送，GitHub Actions 工作流程 `33851477210` 成功；公開網址 `?rev=5edbdff` 的 JS 檔名 `index-_Eah6vlK.js` 與本機一致，內含調整後的備註。

## Phase 30 文字斷行、最新消息區、連署入口、發起團體方格（2026-09-08）
- 目標：Joseph 2026-09-08 回饋（對應 Google 文件「文字排版修改」與「9/4討論」兩個分頁）：(1) 六都現況三格說明、風險表說明、訴求區說明、關於我們說明、加入連署說明依指定位置斷行；關於我們與加入連署同時換新文案。(2) 候選人簽署看板與關於我們之間新增「最新消息」區，卡片含縮圖、日期、類別、標題、摘要，先放 8 月 12 日聯合新聞稿。(3) 右上角「加入連署」直接開連署表單；下方加入連署區的按鈕文字改為「連署影響力」。(4) 發起團體方格改為偏正方形，與連署團體的橫條區隔。
- 斷行做法：沿用 whySixLines 的寫法，文案改成字串陣列，每行一個 `md:block` 的 span，手機仍自然折行。stats.ts 的 description 改為 string[]；demandsLead 改為三段、每段字串陣列；aboutLines 與 joinLines 新增到 site.ts。
- 關於我們的斷行點：照 Joseph 給的位置在「障礙者權益／等領域」中間斷（兩行 43／39 字較均衡；改在「等領域，」後斷會讓第一行 47 字在 5xl 容器內也折行）。斷行只在 lg 以上生效，六都現況三格因欄寬只有約 350px，斷行在 xl 以上才生效，字級縮到 15px 讓最長一行放得下。
- 「連署影響力」：9/4 文件寫的是「加入連署略顯陽春，寫完整一點，包括連署影響力」，今天的指示是「原下方的按鈕文字改成連署影響力」。照字面改下方區塊的按鈕文字，連結仍是 Google 表單；若他的意思是首頁按鈕，待確認。
- 最新消息資料：Google 文件「最新消息」分頁只有標題、gcaa 連結與 TCAN 的合照網址。日期取 gcaa.org.tw/16551 的 article:published_time（2026-08-12），摘要依新聞稿首段改寫；縮圖下載 TCAN 合照後縮成 960px 寬 webp 存進 public/images/news/，不外連。
- 發起團體方格：OrgGrid 加 variant，tile 版為 aspect-[5/4]、名稱置中、三欄（手機兩欄），About 容器縮到 max-w-3xl 讓九個團體排成 3×3。9/4 文件還提到「加團體 logo」，這次沒有 logo 檔，未做。
- 導覽列加「最新消息」，排在候選人承諾之後、關於我們之前，與頁面順序一致。JOIN_SECTION_HREF 不再有人用，移除。
- 驗證（2026-09-08）：`tsc --noEmit` 通過；vitest 11 檔 69 項通過（site.test 改為三段斷行、五處新文案與最新消息區段；新增 News.test）；`npm run build` 通過；`git diff --check` 通過。chrome-devtools 被既有瀏覽器占住，改用 Playwright 檢查本機 dev server：1400px 六都現況三格、風險表、訴求區三段、關於我們、加入連署所有指定行都各占一行；導覽列七個連結加「加入連署」在 1024px 剛好放得下（兩側各 13px）；發起團體 3×3 方格 264×211px；最新消息一張卡片置中；768px 只剩五大訴求既有的兩個長標題折行；390px 無水平溢出、方格兩欄、卡片滿版。右上角「加入連署」在 390px 折成兩行是公開網站既有現象（按鈕高 70px），本次未動。
- 狀態：尚未提交、推送或部署。
- 發布：提交 `411a025` 已推送至 `main`，GitHub Actions 工作流程 `34207133381` 建置與部署成功；公開網址 `?rev=411a025` 的 JS 檔名與本機 dist 一致，內含最新消息、連署影響力、新文案與方格樣式，縮圖 webp 回 200。

## Phase 31 發起團體方格按鈕化比較（2026-09-08）
- 目標：Joseph 希望關於我們區的九個團體方格再矮一點、看起來更像按鈕（hover 整格或左緣強調；風格走平面與現行之間）。這次只做比較原型，未改網站。
- 產出：`design/org-tiles/comparison.html`（獨立頁面，沿用網站色碼與 Noto Sans TC，九個團體在 768px 容器三欄排版），已發布為 artifact https://claude.ai/code/artifact/7024046e-b6ba-49d2-b8ff-f6c5157dc835。頁面上方可切換高度（5:4 現行、3:2、2:1、依內容）與「固定顯示每組第一格 hover」。`comparison-3x2-hover.png` 為 3:2 加固定 hover 的全頁截圖。
- 四個版本：A 整卡淡紫填底 hover（白卡、圓角 12px）；B 左緣 4px 邊條 hover 轉紫（圓角 8px）；C 靜態即淡紫底紫字的平面按鈕，hover 加深（無框、圓角 10px）；D 紫框紫字，hover 反白成深紫底白字。
- 我的建議：A 或 B 搭 3:2（248×165，較現行 211 矮約 46px）。C／D 靜態就是紫色，會和右上「加入連署」按鈕與下方連署團體橫條搶視覺。2:1 時十三字團體名折成兩行後上下只剩約 20px，偏擠。
- 觀察：「台灣氣候行動網絡研究中心」在 18px 三欄下折成「…研究中／心」，最後一字落單，線上版同樣如此；若要處理可縮字級到 17px 或用 `text-wrap: balance`，待定案時一併決定。
- 選定後改動範圍：`OrgGrid.tsx` 的 TILE_BASE 與 hover class；B 版需加左邊框 class；C／D 改字色與字重。
- Joseph 追問 D 能不能更有按鈕立體感。比較頁加了 D1 硬陰影偏移（右下 4px 實色陰影，按下貼平反白）、D2 底邊加厚成鍵帽（按下下沉 3.5px）、D3 柔陰影浮起（hover 上浮、按下內凹）、D4 內光加漸層（擬物上限參考），另加「固定顯示第二格按下狀態」開關；tile 的 transition 補上 box-shadow 與 border-width。artifact 已重新發布，網址不變。
- 立體版本的取捨：D1 最貼合「平面但有立體感」，硬陰影和網站現有的平面插畫語彙相容，但九宮格右欄與底列會多出 4px 要留邊；D2 靜態最像實體按鈕；D3 是一般網頁常見做法，柔影會和米色底糊在一起；D4 已經是擬物，Joseph 之前說不要那麼擬物，放進來只是給上限。
- 2026-09-09 Joseph 選 D2（底邊加厚的鍵帽式按鈕）。高度他沒指定，採之前建議的 3:2。
- 實作：`OrgGrid.tsx` 的 TILE_BASE 改為 aspect 3:2、圓角 10px、紫中框 1.5px、底邊 5px、白底、紫深字、字重 500；方格版連結的互動 class 獨立成 TILE_LINK（hover 框轉紫深、底染紫深 7%；active 底邊縮回 1.5px、下沉 3.5px、整格反白），橫條版的 hover class 獨立成 ROW_LINK 維持原樣；方格版外連箭頭不透明度從 45% 提到 60%。transition 用 transition-all 150ms，reduced-motion 關閉。
- 驗證（2026-09-09）：`tsc --noEmit` 通過；vitest 11 檔 69 項通過；`npm run build` 通過；`git diff --check` 通過。dev server 1400px：方格 264×176（原 211）、圓角 10px、上框 1.5px、底框 5px、框色 purple-mid、白底、字色 purple-deep、字重 500；hover 後底色為 purple-deep 7%、框色 purple-deep；按下時底框 1.5px、translate 3.5px、底色 purple-deep、白字。390px：兩欄、163×109、無水平溢出。連署團體橫條未動。截圖 `design/org-tiles/live-d2-active.png`（第一格按下）與 `live-d2-rest.png`。
- Joseph 確認 3:2；長名稱先說縮到 17px，隨即改為指定斷行點：「台灣氣候行動網絡／研究中心」「台灣身心障礙者／自立生活聯盟」。既然斷行點固定，字級維持原本的 text-base／md:text-lg（17／19px），沒有縮。
- 斷行做法：`Org` 與 `OrgGridItem` 加選填的 `lines` 字串陣列，`orgs.ts` 只在這兩個團體填；OrgGrid 用 `label()` 把各行以 `<br>` 接起來，`name` 仍是完整名稱（key 與連署區都不受影響）。`<br>` 加 `hidden sm:inline`：390px 兩欄格寬 163px，「台灣氣候行動網絡」八個字放不下會變三行，所以手機維持自然折行，sm 以上才強制斷行。
- 驗證（2026-09-09）：`tsc --noEmit`、vitest 11 檔 69 項、`npm run build`、`git diff --check` 通過。dev server：1400px 兩個長名稱各兩行且斷在指定位置，其餘單行，方格 264×176；640px 斷行生效；390px 斷行隱藏、自然折成兩行、兩欄 163×109、無水平溢出。hover 與 active 樣式與前次相同。截圖 `design/org-tiles/live-d2-rest.png`、`live-d2-active.png`、`live-d2-mobile.png`。
- 插曲：改字級時用 `git checkout` 還原檔案，把整個 D2 改動一起還原了，已重新套用並重跑全部檢查。
- 發布：提交 `d36fdd6` 已推送至 `main`，GitHub Actions 工作流程 `34299721689` 成功（2026-09-09 01:34 UTC）。公開網址 `?rev=d36fdd6` 的 JS 檔名 `index-IF4uAQQa.js` 與本機一致且逐位元相同，內含 `lines:["台灣氣候行動網絡","研究中心"]`、`lines:["台灣身心障礙者","自立生活聯盟"]`；CSS 含 aspect 3/2、border-b 5px、active translate-y 3.5px。

## Phase 32 最新消息輪播、區塊底色交錯、簽名圖示換圖（2026-09-09）
- 目標：Joseph 2026-09-09 三項：(1) 測試最新消息有 3／6／12 則、混入投書與無圖項目時的呈現，多則時改輪播而非格狀；(2) 相鄰區塊底色不得相同；(3) 首頁標題旁的簽名圖示與 favicon 換成他提供的手寫插圖（1080×1080 透明底 PNG）。
- 輪播做法：三則以內維持原本置中的格狀；超過三則改成水平捲動的 scroll-snap 軌道，桌機一頁三張、平板兩張、手機一張（85% 寬露出下一張邊緣提示可滑），左右圓形按鈕捲一頁，到頭時停用，手機隱藏按鈕靠滑動。捲軸隱藏（`.news-track`）。
- 無圖項目：`image`／`imageAlt` 改選填，沒有圖時放同比例的淡紫底方塊，中央顯示類別字樣，讓卡片高度對齊。
- 測試資料：不放進 `news.ts`。`src/content/newsMock.ts` 依數量產生假資料（三種類別輪替、每三則一則無圖、日期每週往前推），News 只在 `import.meta.env.DEV` 且網址帶 `?news=N` 時使用，正式建置會被 tree-shake 掉。
- 底色：原本 Demands／Timeline 同為米色、SignBoard／News 同為白 40%、About／Endorsements 同為米色。改成從 DataStory 起嚴格交錯：DataStory 白、Demands 米、Timeline 白、SignBoard 米、News 白、About 米、Endorsements 白、加入連署維持紫 3%。Timeline 與 Endorsements 原本把 max-w 放在 section 上，改成 section 帶底色、內層 div 帶寬度。
- 圖示：原 `SigningHand.tsx` SVG 元件改為 `<img>` 引用 `public/images/signing-hand.png`（裁掉透明邊後縮成 240px 寬），元件與其測試一併移除。favicon 用原圖直接縮成 32／192 與 apple-touch-icon 180，放 `public/` 根目錄，index.html 加三個 link。
- favicon 路徑：index.html 一開始寫 `/six-counties/favicon-32.png`，Vite 對 index.html 內的 link href 會再加一次 base，dev 變成 `/six-counties/six-counties/…`；改成 `/favicon-32.png` 讓 Vite 自己加 base，dev 與 dist 都正確。（news.ts 的圖片路徑是 JS 字串，Vite 不處理，所以那邊要自己寫完整路徑，兩者規則不同。）
- 驗證（2026-09-09）：`tsc --noEmit` 通過；vitest 11 檔 71 項通過（News.test 新增格狀／輪播／無圖三項，iconArtwork.test 移除簽名圖示一項，Hero.test 改查 `alt="簽名圖示"`）；`npm run build` 通過且 dist JS 不含「測試標題」（mock 已被 tree-shake）；`git diff --check` 通過。Playwright 檢查 dev server：1400px 下 `?news=3` 為三欄格狀無按鈕；`?news=6` 軌道 1037px、卡片 329px 一頁三張、捲動寬 2099px 剛好兩頁，按下一頁後顯示第 4–6 則且下一頁按鈕停用；`?news=12` 捲動寬 4224px 四頁；無圖項目顯示淡紫底「投書」字樣色塊，卡片高度與有圖者一致。各區 computed background：top 透明、why-six 白 40%、demands 透明、timeline 白 40%、board 透明、news 白 40%、about 透明、endorse 白 40%、join 紫 3%、footer 紫深，相鄰皆不同。首頁圖示為 240×184 PNG，1400px 下顯示約 37×28、390px 下 32×24。390px：卡片 288px（85%）、無水平溢出、按鈕隱藏靠滑動。favicon 三個檔案與 signing-hand.png 皆回 200。截圖存 `design/news-carousel/`。
- 狀態：尚未提交、推送或部署。
- 發布：提交 `3750cfa` 已推送至 `main`，GitHub Actions 工作流程 `34300740279` 成功（2026-09-09 01:50 UTC，這次不到三分鐘）。公開網址 `?rev=3750cfa` 的 JS 檔名 `index-KxEJeqXM.js` 與本機 dist 一致且 md5 相同（b2c6930b…），不含「測試標題」；HTML 含三個 favicon／apple-touch-icon link 且路徑正確；favicon 三檔與 signing-hand.png 皆回 200。
- Joseph 看過本機預覽後要求無圖佈位改成純灰色塊，不放類別字樣。placeholder 改為 `bg-ink/[0.07]` 的空 div，測試同步改。
- 發布：提交 `77b860c` 已推送至 `main`，GitHub Actions 工作流程 `34301191024` 成功。公開網址 `?rev=77b860c` 的 JS 檔名 `index-D2Fm1wj0.js` 與本機 dist 一致且 md5 相同（24201b5f…）；CSS 含灰色佈位的 7% 不透明度規則。

## Phase 33 最新消息改讀 Google 試算表（2026-09-09）
- 目標：Joseph 2026-09-09 提出：最新消息區的資料改由協作者在共用 Google 試算表填寫（含圖片網址），讓試算表成為這一區的「後台」。
- 決策（Joseph 已確認）：(1) 放在既有「候選人簽署」試算表裡新增工作表「最新消息」，沿用同一個 SHEET_ID 與 gviz 讀法，不另建試算表。(2) 圖片只接受可直接顯示的公開圖片網址，不做 Google Drive 分享連結轉換（Drive 圖片直連不可靠）。已用 curl 驗證 gcaa.org.tw 的 wp-content 圖片帶 Referer 仍回 200，協作者可直接貼文章的特色圖片網址。(3) 加「顯示」勾選欄，未勾選的列不上站，方便先打草稿。(4) 試算表讀取失敗時退回網站內建的 8 月 12 日新聞稿那一則（`src/content/news.ts` 的 NEWS 保留當備援，webp 縮圖也保留）。
- gviz 陷阱：工作表名稱不存在時 gviz 不會報錯，而是回第一個工作表（已用 `sheet=NOPE` 驗證回的是候選人欄位）。所以對應函式要檢查標題列第一格是「日期」，不是就當作讀取失敗，走備援。
- 欄位（8 欄）：日期、類別、標題、摘要、連結、圖片網址、圖片說明、顯示。日期在 gviz 會依儲存格格式回字串（如 2026/8/12），程式統一轉成 YYYY-MM-DD；列依日期新到舊排序，協作者不必自己排。
- 做法：`src/data/sheets.ts` 的 fetchSheet 改為匯出；新增 `src/data/newsSheet.ts`（mapNewsRows、fetchNews）；News 區改用 useSheetData，loading 顯示骨架卡、error 退回 NEWS、empty 維持「目前還沒有消息」。dev 的 `?news=N` 假資料照舊。
- 驗證（2026-09-09）：TDD，先寫 `newsSheet.test.ts`（6 項：日期正規化、標題列守衛、對應、顯示過濾、排序、類別預設）與 News.test 四個狀態測試看它失敗，再實作。`tsc --noEmit` 通過；vitest 12 檔 80 項通過；`npm run build` 通過；`git diff --check` 通過。Playwright 檢查本機 dev server：工作表尚未建立時 gviz 回候選人資料、守衛拋錯、區塊在載入後顯示內建的 8 月 12 日那一則（1 張卡、webp 縮圖、無 aria-busy 殘留）；`?news=6` 假資料仍為輪播 6 張。
- 阻塞：gws 的 OAuth token 失效（invalid_grant / invalid_rapt），建工作表的指令需要 Joseph 重新登入後執行。建表腳本已備在 scratchpad `setup-news-sheet.sh`：新增工作表「最新消息」（凍結標題列、8 欄）、寫入標題列與 8 月 12 日那一則（圖片改用 gcaa.org.tw 特色圖片網址）、A 欄日期格式 yyyy/mm/dd、B 欄下拉三種類別、H 欄勾選框、C／D 欄自動換行、欄寬。
- code-reviewer 審查（2026-09-09）找到一個關鍵錯誤：gviz 偵測到標題列時會把它移到 `table.cols` 的 label，`rows` 裡沒有標題（對照實際回應：cols 是縣市／姓名…，rows[0] 已是資料）。原本用 rows[0][0] === '日期' 當守衛，真工作表建好後會永遠判失敗、永遠退回內建那一則；先前 `sheet=NOPE` 測試過的原因也是錯的（rows[0][0] 是「臺北市」這筆資料，不是標題）。修法：`gviz.ts` 新增 `parseGvizTable` 回 `{cols, rows}`，`parseGviz` 只包一層回 rows；`newsSheet.ts` 標題在 cols 或在 rows[0] 都認得（工作表只有標題列時 gviz 不會偵測標題），兩者都不是才拋錯。
- 同批修正：(1) 日期改從 gviz 的 `Date(年,月,日)` 原值轉 ISO，不再依賴儲存格顯示格式（協作者改格式不會影響排序）。副作用：候選人簽署的「簽署日期」若是真正的日期儲存格，之前拿到的是顯示字串（如 2026/09/15）而 formatSignedDate 對不上就原樣顯示，現在會拿到 ISO 並顯示成「2026 年 9 月 15 日」，這是 formatSignedDate 原本的設計方向。(2) 連結必須 http 開頭才顯示，避免空連結、重複 key 與 javascript: 網址。(3) 測試改用 `parseGvizTable` 解析仿真 gviz 回應，不再手工把標題塞進 rows；News.test 補一項渲染 `<News />` 本體。
- 審查者另建議在退回內建消息時顯示「讀取失敗」提示與重試鈕，好讓維護者察覺；Joseph 已選「退回目前只有一則新聞稿的狀態」，先不加，列為待確認。
- 發布：提交 `fb382c7` 已推送至 `main`，GitHub Actions 工作流程 `34303385853` 成功。公開網址 `?rev=fb382c7` 的 JS 檔名 `index-DZQFmbvu.js` 與本機 dist 一致且 md5 相同（c1afd6da…）。工作表尚未建立，公開站目前走備援、顯示內建那一則，與上線前畫面相同。
- 待辦：Joseph 執行 `! gws auth login` 後，跑 scratchpad 的 `setup-news-sheet.sh` 建工作表，再用 curl 打 gviz `sheet=最新消息` 確認 cols 標籤是「日期…」，並到公開站確認卡片改吃試算表（縮圖網址應變成 gcaa.org.tw 的 jpg）。
- Joseph 追加：要加「讀取失敗」提示（採納審查者建議）。TDD：News.test 先改 error 狀態要有「試算表讀取失敗，以下是網站內建的消息。」與「重新載入」鈕，NewsBody 加 retry prop，News 把 useSheetData 的 retry 傳下去。提交 `e8a4029`，GitHub Actions `34304831318` 成功，公開網址 JS `index-Ddce4rgG.js` 與本機 dist md5 相同（51a132fc…）。
- 工作表建立與驗證（2026-09-09）：Joseph 重新登入 gws 後跑 `setup-news-sheet.sh`，新增工作表「最新消息」（sheetId 398357285）：8 欄標題列加粗淡紫底並凍結、A 欄日期格式、B 欄下拉三類別、H 欄勾選框、C／D 欄自動換行、欄寬；第 2 列寫入 8 月 12 日聯合新聞稿，縮圖改用 gcaa.org.tw 的特色圖片網址。gviz 驗證：cols label 為 日期…顯示，rows[0] A 欄 `Date(2026,7,12)`、H 欄 true（標題列確實被收進 cols，證實審查者的判斷）。公開站 DOM：卡片連結 gcaa.org.tw/16551、img src 為 gcaa.org.tw 的 jpg、捲到區塊後 naturalWidth 1920（從 github.io 頁面直連可載入）、alt 與日期正確、無讀取失敗提示。草稿測試：第 3 列填一筆 顯示=FALSE，gviz 回 false（程式端過濾有單元測試），測完已清掉。試算表裡另有建表時就存在的空白「工作表1」，未動。
- 待辦：協作者填表說明在 docs/apps-script-deploy.md「最新消息工作表」，可直接轉給同事；請同事用 GCAA 帳號被加為該試算表編輯者。

## Phase 34 加入連署區按鈕文字改回「加入連署」（2026-09-09）
- Joseph 指示：「連署影響力 文字 改回 加入連署」。只改 `src/content/site.ts` 的 joinCta 與 site.test 對應斷言；右上角導覽列按鈕本來就是「加入連署」，不動。
- 發布：提交 `f2675d7` 已推送至 `main`，GitHub Actions `34305783597` 成功；公開網址 JS `index-Co8bAL_C.js` 與本機 dist 一致，內容已無「連署影響力」。

## Phase 35 候選人簽署看板改為截止公告（2026-09-15）
- Joseph 指示：導覽列的「候選人承諾」連結保留；看板本體先改成一句公告「候選人簽署至 9/28 截止，完整簽署結果將於截止後公開。」；原有設計註解保留，不刪。
- 做法：`SignBoard.tsx` 改成只渲染區塊 id、標題與公告；原本整份檔案（縣市篩選、骨架卡、四種狀態、候選人卡片）放在檔尾的區塊註解裡，日後解除註解、刪掉上面的精簡版即可還原。props 介面照舊，`App.tsx` 的接線不動（首頁統計盒仍要用同一份試算表資料）。公告文字放 `site.ts` 的 `boardNotice`，`boardLead` 留著給還原用。
- 副作用：CandidateCard 沒有其他引用，正式建置會被 tree-shake 掉，dist JS 已不含「依縣市篩選」。
- 未動、待 Joseph 決定：首頁統計盒仍顯示「候選人簽署共 N 位」與「看六都市長候選人簽署結果」按鈕，兩者都連到看板；看板現在只剩公告，若試算表在 9/28 前已有簽署資料，首頁會先透露數字。若要一致，可暫時把首頁的候選人數字也改成公告或隱藏。
- 驗證（2026-09-15）：`npm ci` 後 `tsc --noEmit`、vitest 13 檔 86 項（新增 `SignBoard.test.tsx` 2 項、site.test 加 boardNotice 斷言）、`npm run build`、`git diff --check` 通過。尚未提交、推送或部署。
- Joseph 決定：首頁統計盒先顯示「候選人簽署共 ？ 位」。`App.tsx` 把 signedCount 固定為「？」，原本從試算表計算的邏輯註解保留；Hero 的 count 型別放寬為 number | string | null。Hero.test 加一項。vitest 13 檔 87 項、tsc、build、diff --check 通過。
- 發布：提交 `3cde176` 已推送至 `main`，GitHub Actions `34920501064` 成功（2026-09-15 02:16 UTC）。公開網址 https://litostswirrl.github.io/six-counties/ 的 JS `index-DaxqPiG9.js` 與本機 dist md5 相同（148bee2c…），內含截止公告、不含「依縣市篩選」。

## Phase 36 網站搬到 gcaa.org.tw 子網域（2026-09-16）
- Joseph 指示：不搬 Firebase，留在 GitHub Pages，改掛 gcaa.org.tw 子網域；名稱選定 `sixcounties.gcaa.org.tw`。
- 事前查證：GitHub Pages 設好自訂網域後，舊網址會自動 301 轉到新網址且保留路徑（以 twbs.github.io/bootstrap/ 實測，轉到 getbootstrap.com 同路徑）。gcaa.org.tw 的 DNS 由自家 ns.gcaa.org.tw 管，紀錄要請 GCAA 網域管理者加。repo 的 Pages 設定可用 `gh api`（LitostSwirrl 帳號）操作。
- 順序（不能顛倒，否則公開站會先斷）：(1) 管理者加 CNAME `sixcounties` → `litostswirrl.github.io`；(2) `dig sixcounties.gcaa.org.tw` 解析到 github.io 後，同一次完成：Pages 設定 cname、`vite.config.ts` base 改 `'/'`、推送部署；(3) GitHub 簽好憑證後開 Enforce HTTPS；(4) 驗證新網址與舊網址轉址。
- 先做的準備：src 裡五處圖片路徑原本寫死 `/six-counties/images/...`（site.ts 兩處、news.ts、newsMock.ts、Hero.tsx），改成 `import.meta.env.BASE_URL` 前綴，對應的 site.test 與 News.test 一起改。這步不依賴 DNS，現在部署後產出與原本相同，之後只需改 base 一行。
- 給管理者的 DNS 申請文字已 pbcopy。
- 驗證（2026-09-16）：`tsc --noEmit`、vitest 13 檔 87 項、`npm run build`、`git diff --check` 通過。建置出的 `index-DaxqPiG9.js` 與 phase 35 上線版檔名相同，代表這次改動對正式站零影響。另把 base 暫改 `'/'` 排練：87 項測試仍通過，dist 內四個圖片路徑全部變成 `/images/...`、index.html 的 JS 路徑為 `/assets/...`；排練完已還原 base。
- 發布：提交 `b7f4424` 已推送至 `main`，GitHub Actions `35050202103` 成功；公開網址 `?rev=b7f4424` 的 JS 仍是 `index-DaxqPiG9.js`，與上線版一致。
- 阻塞：等 GCAA 網域管理者加 CNAME。`dig +short sixcounties.gcaa.org.tw` 目前無結果。
- DNS 生效後的切換步驟（一次做完）：
  1. `dig +short sixcounties.gcaa.org.tw` 應回 `litostswirrl.github.io.` 與四個 185.199.x.x IP
  2. `gh api -X PUT repos/LitostSwirrl/six-counties/pages -f cname=sixcounties.gcaa.org.tw`
  3. `vite.config.ts` 的 base 改 `'/'`，跑 tsc、vitest、build，確認 dist 圖片路徑為 `/images/...`，提交推送
  4. 等 `gh api repos/LitostSwirrl/six-counties/pages` 的 `https_certificate.state` 變 `approved` 後 `gh api -X PUT repos/LitostSwirrl/six-counties/pages -F https_enforced=true`
  5. 驗證：新網址 1200px／390px 畫面、四張圖片載入；`curl -sI https://litostswirrl.github.io/six-counties/` 回 301 到新網址；`#join` 錨點跳轉正常
- 改向（2026-09-16）：Joseph 認為網站是九個團體共同的，掛在 gcaa.org.tw 下不妥；Firebase 也考慮過（預設網址仍帶 .web.app，免費方案每日 360 MB 流量上限，約 300 到 400 次首次造訪就會斷站），最後決定改用中立的 GitHub 組織 `six-counties-2026`，網址 `https://six-counties-2026.github.io/`。不涉及任何 DNS 紀錄；先前 pbcopy 的 DNS 申請文字作廢，不要寄。
- 名稱查證：`gh api users/six-counties-2026` 回 404，名稱可用。
- 做法（避免舊網址斷線）：不用 transfer（GitHub 文件說轉移不會轉址 Pages）。改為 (1) Joseph 在瀏覽器建組織（API 只開放企業方案建組織）；(2) 在組織下建 `six-counties-2026.github.io` repo，把本 repo 推上去，base 改 `'/'`，用同一份 Actions 部署，驗證新網址；(3) 舊 repo `LitostSwirrl/six-counties` 的 main 換成一頁轉址 stub（meta refresh 加 JS，保留 hash 錨點），部署後封存；(4) 本機 remote 改指向新 repo。
- 再改向（2026-09-16）：Joseph 指示 repo 整個搬到 GCAA。GitHub 上的 GCAA 組織是 `gcaa-org-tw`（LitostSwirrl 為 admin），已用 `gh api repos/LitostSwirrl/six-counties/transfer` 轉移，新 repo `gcaa-org-tw/six-counties`，Pages 設定（workflow 建置、強制 HTTPS）隨 repo 帶過去。網址路徑 `/six-counties/` 不變，只有主機名變，`vite.config.ts` base 不必動，圖片路徑改 BASE_URL 那步順便保險。
- 新公開網址：https://gcaa-org-tw.github.io/six-counties/ 。手動觸發 Actions `35050838907` 成功；curl 確認首頁 200、JS 仍為 `index-DaxqPiG9.js`、四張圖片 200。
- 舊網址轉址：GitHub 轉移 repo 不會轉址 Pages，所以在 `LitostSwirrl/six-counties` 原名新建一個只放 index.html／404.html 的 stub repo（meta refresh 加 JS，保留路徑、query 與 hash；robots noindex），branch 部署。Playwright 實測 `litostswirrl.github.io/six-counties/?rev=test#join` 落在 `gcaa-org-tw.github.io/six-counties/?rev=test#join`，標題正確；深層路徑走 404.html 一樣轉。
- 順帶觀察（非本次範圍、遷移前就如此）：帶 `#join` 直接開新網址時 scrollY 仍為 0，錨點沒有在載入時捲到連署區；導覽列點擊的捲動不受影響。
- 本機 remote 已改為 gcaa-org-tw；repo 內 README／docs 沒有寫死 litostswirrl 網址。README 第 28 行提到掛 gcaa.org.tw 子網域的做法已不適用，未改。
- Joseph 決定（2026-09-16）：在 gcaa-org-tw 之上再掛 `sixcounties.gcaa.org.tw`。DNS 申請文字已重新 pbcopy，CNAME 目標改為 `gcaa-org-tw.github.io`（不是先前的 litostswirrl）。
- 切換步驟改為：(1) `dig +short sixcounties.gcaa.org.tw` 應回 `gcaa-org-tw.github.io.`；(2) `gh api -X PUT repos/gcaa-org-tw/six-counties/pages -f cname=sixcounties.gcaa.org.tw`；(3) base 改 `'/'`，tsc、vitest、build，確認 dist 圖片路徑 `/images/...`，提交推送；(4) 憑證 approved 後 `-F https_enforced=true`；(5) 驗證新網址、`gcaa-org-tw.github.io/six-counties/` 回 301 且保留路徑；(6) 把 `LitostSwirrl/six-counties` stub 的目標改成新網址，少走一跳。
- 再改向（2026-09-16）：Joseph 決定網址用 `six-cities.gcaa.org.tw`（訊息寫成 six-cities@gcaa.org.tw，@ 視為 . 的筆誤），並提供 GCAA 主機與網域的登入資料，要我直接操作介面。登入資料只在對話裡，不寫進任何檔案。
- DNS 在哪：`ns.gcaa.org.tw` 就是 PUMO 的 Plesk 主機 linplesk43（124.150.132.83），zone 由 Plesk「DNS 設定」管理；zone 內原本就有 `thaubing-esg`、`data.thaubing-esg` 兩筆 CNAME 指向 `gcaa-org-tw.github.io`，以及 GitHub 組織網域驗證的 `_gh-gcaa-org-tw-o` TXT。用 chrome-devtools 登入 Plesk，新增 CNAME `six-cities` → `gcaa-org-tw.github.io`、TTL 3600，按「更新」套用（Plesk 新增後要另外按更新才會寫進 zone），完成後登出。權威伺服器與 8.8.8.8 立即解析到 GitHub 四個 185.199.x.x IP。
- GitHub 端：`gh api -X PUT repos/gcaa-org-tw/six-counties/pages -f cname=six-cities.gcaa.org.tw`；`vite.config.ts` base 改 `'/'`（tsc、vitest 87 項、build 通過，dist 圖片路徑 `/images/...`、JS `/assets/index-acUvRcRu.js`），提交 `f0dbba3`，Actions `35056262880` 成功；憑證幾分鐘內 approved（Let's Encrypt，到期 2026-12-15），隨即 `https_enforced=true`，約一分鐘後 http 回 301 到 https。
- 舊網址：`gcaa-org-tw.github.io/six-counties/` 由 GitHub 自動 301 到新網域，路徑與 query 保留；`LitostSwirrl/six-counties` stub 改為直接指向新網域（少走一跳）。Playwright 實測 `litostswirrl.github.io/six-counties/?rev=final#join` 落在 `six-cities.gcaa.org.tw/?rev=final#join`。
- 切換後 10 分鐘內的假象：瀏覽器開 `gcaa-org-tw.github.io/six-counties/` 仍看到舊頁而且 JS 404。查證是 GitHub CDN（Fastly）對 gzip 變體還留著舊建置的 200（cache-control max-age=600、age 550），curl 不帶 Accept-Encoding 的變體早已是 301。等到期後所有變體都回 301，用隔離的乾淨瀏覽器 context 重測正常。不是程式問題，記下來免得下次白查。
- README 第 28 行的部署段落改成現況。
- 驗證清單（2026-09-16）：新網址首頁 200、標題正確、JS 與四張圖片 200；http→https 301；舊兩個網址都到新網址且保留 `?rev` 與 `#join`；Plesk 已登出。
